# YuE任务服务与Runner MCP设计交付包 v1.6

**设计修订，未发布endpoint、修改业务代码或运行模型。**

## 本次修正

只把四个Runner能力暴露为MCP工具：runner_upload、runner_submit、runner_result、runner_result_source。MCP与HTTP共用上传、提交、查询、资源打开及认证函数；原模型Task/preprocess/environment保持不变。

v1.4的MCP执行后端方向撤回；本包以v1.3已确认业务设计为基础加入正确的MCP对外封装，不保留错误的工作流配置、后端工具调用或文件桥接样例。先前包保留为历史记录，不应继续用于MCP实施。

## 交付清单

| 文件／目录 | 内容 |
|---|---|
| 01_技术设计稿.md | 原服务设计加MCP入口说明，四个Runner能力与原模型绑定 |
| 02_文件级实施清单.md | 原模型适配和本次API封装文件级改动 |
| 03_验证计划与实际检查.md | 原V01–V18及本次M01–M12验证入口 |
| 04_符合性审查与决策.md | 删除错误执行方向，保留所需协议封装 |
| 05_业务时序图.md、diagrams/ | 原三条业务流程及第四条Runner MCP文件传输流程 |
| 07_Claude_plugin_Skill封装设计.md | 同一Skill业务步骤，HTTP/MCP调用映射 |
| 08_模型环境与任务绑定修订.md | 原模型独立环境和任务绑定，不因MCP改动 |
| 09_通用上传与任务输出资源契约修订.md | 原用户隔离、asset_id及task_id/result_source_name契约 |
| 10_Runner接口MCP封装与文件传输.md | 本次完整修订：四工具、文件适配、错误权限、配置、改动表与M01–M12 |
| examples/mcp/ | 四工具Schema、调用报文、小文件真实校验样例、链接及超限错误 |
| examples/sandbox.mcp.example.yaml | 仅向bootstrap增加mcp的配置增量 |
| evidence/、tools/ | 来源及静态检查；脚本不是服务业务实现 |
| 阅读版.html | 离线阅读；Mermaid源码展示，不声称已渲染 |

## 文件传输与验收边界

MCP 上传采用一次性、短效 PUT URL：工具只签发地址和本地 `curl` 命令，本地客户端主动读取文件并上传；服务临时落盘后调用共享上传函数，返回资产标识才算完成。下载仍按大小选择内联字节或原 HTTP 流，资源链接明确 `bytes_included=false`。共享同一资产／任务标识，不把客户端路径当作服务端路径，不引入第二套文件服务。

当前只验证本包的示例结构、样例字节、配置和文档一致性，不证明HTTP/MCP协议或真实Task运行。M01–M12及模型验收均未运行。原evidence/revision-checks.json记录属于v1.3历史检查，本次结果单独存放。
