# 文件级实施清单

**状态：v1.3 实施规格，未执行业务代码改造。** 路径带“新增”为拟新增；已有路径均来自固定源码。序号与验证编号对应03文档。本文不要求同时修改RVC-Speakers，也不修改YuE核心模型算法。

## A. agent-sandbox（主改造仓库）

| 编号 | 路径与状态 | 类/函数/配置 | 改动、必要性 | 兼容影响 | 验证 |
|---|---|---|---|---|---|
| A01 | `sandbox.yaml`（已有） | preprocess/tasks/bootstrap | 为两个模型Processor配置environment、cwd、权重；分别绑定yue2_task、sheetsage2_task；单实例max_ongoing_tasks=1部署样例 | 不全量覆盖现有业务配置；旧Processor条目保持 | V01,V02,V11 |
| A02 | `sandbox/processors/__init__.py`（已有） | load_preprocess、导入与__all__ | 导入两个新类触发注册；沿用from_config和cache | 不变更所有旧构造函数或调用签名 | V02,V03 |
| A03 | `sandbox/tasks/__init__.py`（已有） | load_task、任务导入 | 分别注册YuE2Task/yue2_task和SheetSage2Task/sheetsage2_task；每个只绑定本模型preprocess | 旧Task保留 | V02,V03 |
| A04 | `sandbox/server/model/music.py`（新增） | YuE2Submission、SheetSage2Submission、NativeSongRequest、ResourceRef、TranscriptionOptions、ScoreCheckOptions | 按task_name区分的strict校验、参数规范化、task_name/operation组合白名单、业务字段与执行字段分离 | 不替换PayLoad或旧Serializable；不忽略未知音乐字段 | V03,V04,V05 |
| A05 | `sandbox/processors/music_runtime.py`（新增） | resolve_python_environment、preflight、run_model_process | 明确解释器、受控argv、cwd/env、stdout/stderr、deadline和进程组清理；两个Processor共享 | 不改旧评测静默超时；无environment的旧任务不受影响 | V06,V07,V08 |
| A06 | `sandbox/processors/yue2_processor.py`（新增） | YuE2Processor.from_config/init_paths/__call__ | generate/plan/all_modes/decode/score_check映射真实helper；模型路径与请求目录；结果分类 | 只由yue2_task调用；不导入GPU模型到服务进程 | V03,V04,V06,V09,V13 |
| A07 | `sandbox/processors/sheetsage2_processor.py`（新增） | SheetSage2Processor.from_config/init_paths/__call__ | transcribe参数映射；独立环境、父模型路径与可用ABC检查 | 不新增独立MERT任务；不将max_seconds视为内存开关 | V04,V06,V13,V15 |
| A08 | `sandbox/processors/music_artifacts.py`（新增） | collect_music_artifacts、publish_music_result、verify_native_result | 原生manifest与helper状态校验；result_source_name到内部result路径键的映射、MIME、SHA256、原子收尾；保留partial/needs_review | 内部保留既有四路径；不把大文件塞入JSON | V09,V10,V12 |
| A09a | `sandbox/tasks/yue2_task.py`（新增） | YuE2FlowData、YuE2Task.prepare/from_config/dispatch | 仅绑定yue2_processor；允许generate/plan/all_modes/decode/score_check；从内部_service取相同task_id | 沿用旧Task协议；拒绝transcribe及跨模型绑定 | V02,V03,V06,V07,V09,V11 |
| A09b | `sandbox/tasks/sheetsage2_task.py`（新增） | SheetSage2FlowData、SheetSage2Task.prepare/from_config/dispatch | 仅绑定sheetsage2_processor；仅允许transcribe；独立参数模型与生命周期记录 | 不依赖YuE2Task实例；拒绝generate及跨环境请求 | V02,V03,V06,V07,V09,V11 |
| A09c | `sandbox/tasks/music_result.py`（新增） | save_music_result | 两个模型Task共用结果投影函数，不注册通用Task，不负责跨模型派发 | 现有progress签名和旧任务不变 | V07,V09 |
| A10 | `sandbox/server/music_task_store.py`（新增） | create_or_get、read、update、recover、prune | 本机音乐任务记录、去重、终态读盘、与现有维护循环整合 | 仅yue2_task与sheetsage2_task使用；不迁移所有旧队列/历史任务 | V05,V10,V12 |
| A11 | `sandbox/server/music_auth.py`（新增） | require_music_principal、authorize_music_resource | 服务令牌摘要映射、主体绑定与资源校验；拒绝无认证音乐请求 | 音乐部署默认开启；旧部署安全状态另行说明 | V05,V10,V16 |
| A12 | `sandbox/server/servlet/extract_file.py`（已有） | 拟新增 upload_runner_file | 通用POST /runner/upload：file/user_id、独立asset_id目录、流式大小/SHA256和原子登记 | 参考extract_standard_file用户隔离，不调用其删除/解压逻辑，不按模型命名 | V05,V10 |
| A12b | `sandbox/server/runner_assets.py`（拟新增） | register_uploaded_asset、resolve_uploaded_asset | 上传与Task共享的user_id/asset_id归属、相对路径、摘要、引用及清理检查；不注册新服务或队列 | 不放进music专用路由；任务prepare仍纯转换，dispatch准备阶段取得受控文件 | V05,V10 |
| A13 | `sandbox/server/servlet/runner.py`（已有） | submit_async、result_async、result_source_async、post_task_update_async | 用现有路由接两个模型Task；音乐入口强制任务类型白名单；主体校验/Idempotency-Key/内部_service；按清单下载、完整结果投影；GET只读，按task_id+result_source_name重取文件 | 所有改变限制在yue2_task / sheetsage2_task白名单；旧code/msg/data字段保留；音乐错误使用真实HTTP状态 | V03,V05,V09,V10,V12 |
| A14 | `sandbox/server/servlet/boot/runner_bootstrap.py`（已有） | RunnerBootstrapBaseWeb.from_config/run | 接收bootstrap.upload与bootstrap.music；仅新增通用/runner/upload，输出复用两个GET，安装权限边界 | 新配置不能被默默忽略；旧bootstrap无music保持可启动 | V01,V02,V10 |
| A15 | `sandbox/server/server_init.py`（已有） | start_async_app/dispatch | 音乐能力就绪检查、记录恢复/清理；Worker意外退出后暂停再派发而非带遗留模型重启 | 不在旧非音乐模式下任意改重启策略；保留原启动入口 | V01,V08,V11,V12 |
| A16 | `sandbox/start/core.py`（已有） | preparation_runner、sync_state、listen | 校验双prepare身份；内部HTTP改为非阻塞调用或to_thread；终态重试有次数/退避，失败留manifest供API修复；取消finally清理 | hooks签名保持；用回归测试覆盖旧任务；不得用RVC旧代码覆盖 | V03,V07,V08,V11,V12 |
| A17 | `tests/music/`（新增） | 见03列出的测试文件 | 参数、环境、生命周期、权限、产物、幂等、旧任务回归 | 不以假模型测试冒充AutoDL验证 | V01–V13 |
| A18 | `docs/music-service.md`（新增） | 服务契约及部署说明 | 固定操作、原生与服务参数边界、记录保留、凭据和任务恢复 | 标注上游版本，不复制无关旧路径文档 | V17,V18 |

## B. YuE（上游仓库）

| 编号 | 路径 | 处理决定 | 必要性与验收 |
|---|---|---|---|
| B01 | `skills/yue2-music/scripts/run_yue2.py`（已有） | 固定版本复用，不改业务实现 | --model/--vae/--offline已经满足本地路径需求；不要新增假参数 [S19] |
| B02 | `skills/yue2-music/scripts/transcribe.py`（已有） | 固定版本复用 | --base-model可指定离线父模型；验证模型接口与权重revision [S25] |
| B03 | `skills/yue2-music/scripts/abc_tools.py`（已有） | 由服务受控执行，保留source/new-output规则 | 使用inspect/strip-chords/compare；不新增自己的乐谱对齐模型 [S27] |
| B04 | `src/yue2/storage.py`、`pipeline.py`（已有） | 不改代码；通过本地路径与环境变量控制 | 验证最终resolve路径、pipeline.json重定向与完整性 [S21][S22] |
| B05 | 部署侧 `/root/checkpoint/.hf`、`model-revisions.json`（拟议部署文件） | 记录实际下载的模型revision、父版本与校验信息 | 本轮没有模型实体，不写入虚构revision或下载成功记录 |

若固定上游脚本在真实AutoDL出现确证错误，再以独立可审阅补丁处理；不能在方案阶段先发明一套替代模型接口。RVC-Speakers本期无改动文件。

## C. Claude plugin Skill 封装文档

完整说明见《07_Claude_plugin_Skill封装设计》。以下是后续封装内容规格；本轮只交付文档，不生成插件发行包，也不增加独立客户端实现清单。

| 路径 | 职责 | 验收 |
|---|---|---|
| `.claude-plugin/plugin.json` | 插件名称、版本、描述 | 符合官方目录和元数据格式 [W01][W02] |
| `skills/yue2-music/SKILL.md` | 保留原步骤；将模型执行入口替换为任务服务 | 生成、转录、翻唱、编辑、检查、试听比较不遗漏 [S17] |
| `skills/yue2-music/assets/prompt.json` | 保留原生请求模板 | 不混入服务器执行配置 |
| `skills/yue2-music/references/service-api.md` | 说明四个接口、任务绑定、输入与结果引用 | upload/submit/result/result_source 名称和报文一致 |
| 原 Skill 实际引用的参考文档及非模型辅助文件 | 保留原编辑约束、试听比较与来源说明 | 不新建客户端系统或播放器业务模块 |

## D. 依赖顺序与验收门

**门1：代码基线。** 实施机确认包含PR11修复，保留未提交修改。

**门2：任务服务。** A01–A16形成实际调用链，V01–V13验证配置、参数、文件、生命周期和权限。

**门3：真实模型。** 分别验证两套环境及生成、转录、翻唱、编辑结果，完成V14–V16。

**门4：Skill封装与步骤验收。** 按已验证服务契约修改原Skill的执行入口；V17检查封装格式与步骤对应，V18验证完整流程、恢复查询和同一资源下载重试。保留原试听比较，不引入额外应用开发工作。

本轮为设计交付；门2–门4未实际执行，不能凭文档存在标记通过。

<!-- 固定源码/官方资料引用 -->
[S17]: https://github.com/multimodal-art-projection/YuE/blob/bd90e4ccae671d869b3ecaca6d7e893927d29442/skills/yue2-music/SKILL.md
[S19]: https://github.com/multimodal-art-projection/YuE/blob/bd90e4ccae671d869b3ecaca6d7e893927d29442/skills/yue2-music/scripts/run_yue2.py
[S21]: https://github.com/multimodal-art-projection/YuE/blob/bd90e4ccae671d869b3ecaca6d7e893927d29442/src/yue2/pipeline.py
[S22]: https://github.com/multimodal-art-projection/YuE/blob/bd90e4ccae671d869b3ecaca6d7e893927d29442/src/yue2/storage.py
[S25]: https://github.com/multimodal-art-projection/YuE/blob/bd90e4ccae671d869b3ecaca6d7e893927d29442/skills/yue2-music/scripts/transcribe.py
[S27]: https://github.com/multimodal-art-projection/YuE/blob/bd90e4ccae671d869b3ecaca6d7e893927d29442/skills/yue2-music/scripts/abc_tools.py
[W01]: https://code.claude.com/docs/en/plugins
[W02]: https://code.claude.com/docs/en/plugins-reference

## E. v1.5 Runner MCP对外封装

新增的只是API协议适配，文件级清单见[10§8](10_Runner接口MCP封装与文件传输.md)。拟新增mcp_api.py；必要时提取runner_service.py；路由、通用上传、资源打开和认证共用原业务函数。在现有bootstrap挂载/mcp，不增加任何Task/Processor注册，不改造参考仓库。
